<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sashashtmv.fastcredit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ServiceActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".credits.CalculatorActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".credits.CreditListActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".loans.LoanListActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".cards.CardListActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".other.OtherListActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>